""" Main script with math metods"""
from typing import Union


def multiply_3(n: Union[int, str]) -> int:
    """Multiply 3 to n.

     :param n: number to multiply 3 to
     :return: n * 3
     """

    return int(n) * 3
